{% for i in range(0, 25) %}
podcast{{ i }}:
  title: {{ faker.sentence(nb_words=10) }}
  description: {{ faker.sentence(nb_words=40) }}
  audio_file: fixture.mp3
  author: {{ random_model('User') }}
{% endfor %}